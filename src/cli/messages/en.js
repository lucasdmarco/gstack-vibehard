/**
 * PRD48 S48.6 — English catalog. Same stable IDs as pt-BR.js — text can change freely,
 * the ID never does. JSON contracts never translate keys/enums — only human text does.
 */
export default {
  "task.session_not_found": "session not found: {sessionId}",
  "task.checkpoint.confirmation_required": "restore to checkpoint {seq} of run {runId}? run again with --yes to confirm.",
  "task.checkpoint.restore_failed": "restore failed: {reason}",
  "task.checkpoint.restored": "restored to checkpoint {seq} ({count} file(s); receipt {receipt})",
}
