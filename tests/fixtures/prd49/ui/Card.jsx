// Fixture de UI mínima — usada pelos sprints futuros (49.2B) do detector de design.
export function Card({ title }) {
  return <div style={{ color: "#ff00aa", borderRadius: "17px" }}>{title}</div>
}
