# Regra declarativa de design (fixture segura)

Texto de regra sem sinal de risco — sem rede, sem instalação, sem segredo, sem exec remoto.
Usado para provar que `vendorSafetyCheck` classifica conteúdo limpo como `ok`.
